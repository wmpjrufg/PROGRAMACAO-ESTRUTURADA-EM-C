#include <stdio.h>
//#include <stdlib.h>

main (void) {
   int a, b;
   scanf ("sdsd %d sds %d", &a, &b);
   double media;
   media = (a + b)/2.0;
   printf ("A média de %d e %d é %f\n", a, b, media);
   //return EXIT_SUCCESS;
}
